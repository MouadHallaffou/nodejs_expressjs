<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Home Page</h1>
    <% if (typeof editData !== 'undefined') { %>
      <form action="/edit/<%= editData._id %>?_method=PUT" method="post">
        <input type="text" name="name" value="<%= editData.name %>" required />
        <button type="submit" style="background-color: rgb(124, 124, 253);">Update</button>
        <a href="/">Cancel</a>
      </form>
    <% } else { %>
      <form action="/" method="post">
        <input type="text" name="name" placeholder="Enter your name" required />
        <button type="submit" style="background-color: rgb(96, 151, 247);">Submit</button>
      </form>
    <% } %>

    <% if (typeof message !== 'undefined') { %>
      <p style="color: green;"><%= message %></p>
    <% } %>

    <h2>Submitted Names:</h2>
    <% if (arr.length > 0) { %>
    <table border="1">
      <thead>
        <tr>
          <th>#</th>
          <th>ID</th>
          <th>Name</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% arr.forEach(el => { %>
        <tr>
          <td><%= arr.indexOf(el) + 1 %></td>
          <td><%= el._id %></td>
          <td><%= el.name %></td>
          <td>
            <form action="/delete/<%= el._id %>?_method=DELETE" method="post" style="display:inline;">
              <button type="submit" style="background-color: rgb(244, 96, 96);">Delete</button>
            </form>
            <form action="/edit/<%= el._id %>" method="get" style="display:inline;">
              <button type="submit" style="background-color: rgb(124, 124, 253);">Edit</button>
            </form>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
    <% } else { %>
    <p>No names submitted yet.</p>
    <% } %>
  </body>
</html>
